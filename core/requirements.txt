# STEP D Core — AI Speech Recognition & Clip Recommendation Pipeline
# Based on VideoLingo's WhisperX pipeline reference (Apache 2.0).

# ── Production deps (GPU-free — runs on the worker VM) ───────────────────────────
# STT (default STT_PROVIDER=gemini), refine, vision, name-OCR, recommend all use
# Gemini on Vertex AI. Auth via ADC (gcloud auth application-default login), no API key.
google-genai>=1.0.0

# Scene segmentation (scenes.py) — shot-change detection + frame extraction.
# opencv also powers the algorithmic scene pre-filter (prefilter.py): Haar face count +
# Farneback optical-flow motion + Canny caption-band density, to send only the top-N
# frames to Gemini Vision.
scenedetect>=0.6
opencv-python>=4.8

# Optional: audio signal for the scene pre-filter (prefilter.py) — librosa onset strength.
# Absent → the audio component degrades to a numpy RMS fallback (still works). ISC license.
#   pip install librosa>=0.10

# YouTube download (downloader.py)
yt-dlp>=2024.0

# ffmpeg/ffprobe must be on PATH (system install), used for audio/frame extraction.

# ── Optional: faster-whisper (MIT) — STT provider AND the Gemini-STT fallback ────
# Two uses, both optional:
#   1) STT_PROVIDER=whisper — run it as the primary (needs CUDA for large-v3 float16).
#   2) STT_FALLBACK=whisper (default) — CPU int8 last-resort when Gemini STT returns
#      nothing. CPU-only, NO CUDA needed; slow on long clips but keeps a transcript.
# Absent → the fallback is simply skipped (pipeline continues transcript-free).
#   pip install faster-whisper>=1.0.0                     # enables the CPU int8 fallback
#   pip install nvidia-cudnn-cu12==8.9.7.29   # only for CUDA float16 (9.x fails — see WHISPERX_GUIDE.md)

# ── Admin lab server (apps/server serves it; only if running the Python admin) ──
# fastapi / uvicorn — not needed in production (apps/server is the one backend).
